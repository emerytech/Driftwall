# Build output
Driftwall.app/
Driftwall.saver/
Driftwall-MAS.app/
*.zip
dist/

# Generated icons (build.sh regenerates from tools/MakeIcons.swift)
Resources/AppIcon.icns
Resources/MenuBar.pdf

# Vendored Sparkle binary + tools (build.sh re-fetches if missing)
vendor/

# Local, untracked signing/notarization config (never commit)
config.local.sh

# macOS cruft
.DS_Store
